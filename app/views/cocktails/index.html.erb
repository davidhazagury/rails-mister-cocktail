<div class="banner">
  <h1>
    Welcome to Mr. Cocktail
  </h1>
  <div class="punchline">
    <p>Imagine</p>
    <p>Create</p>
    <p>Drink</p>
  </div>
  <%= link_to "Create a new cocktail", new_cocktail_path, class: "btn-banner" %>
</div>

<div class="cocktail-list">
  <div class="cards">
    <% @cocktails.each do |cocktail| %>
    <%= link_to  cocktail_path(cocktail) do%>
       <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= cl_image_path cocktail.photo.key, :cloud_name => dgsqurpio %>)">
         <%= cocktail.name %>
       </div>
       <% end %>
      <% end %>
  </div>
</div>
